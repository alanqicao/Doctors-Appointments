
<h1>Doctors</h1>
<%= link_to 'Add doctor', new_doctors_path %>
<hr />
<h2><%= @doctors.length <= 0 ? "No doctors" : "" %></h2> 
<ul>
  <% @doctors.each do |doctor| %>
    <li>
      <%= link_to doctor.name, doctors_path(doctor) %>
    </li>
  <% end %>
</ul>